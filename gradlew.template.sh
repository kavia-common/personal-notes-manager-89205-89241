#!/usr/bin/env sh
# Canonical template for root-level gradlew stub used in CI.
# This is a no-op to satisfy CI pipelines that call ./gradlew at repo root.
echo "[CI stub] ./gradlew executed (from template). Expo managed repository; no native Gradle build."
exit 0

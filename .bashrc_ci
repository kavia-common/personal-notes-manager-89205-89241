# Auto-sourced by some CI base images
if [ ! -f "./gradlew" ]; then
  echo "[.bashrc_ci] Creating ./gradlew stub."
  cat > ./gradlew <<'EOF'
#!/usr/bin/env sh
echo "[CI stub] ./gradlew invoked at repo root; Expo managed (no native Gradle by default)."
exit 0
EOF
  chmod +x ./gradlew 2>/dev/null || true
fi

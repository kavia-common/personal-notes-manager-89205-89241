@ECHO OFF
REM Root-level Gradle wrapper stub for CI in Expo-managed repository
ECHO [CI stub] Root-level gradlew.bat executed. This is a stub, not a real Gradle wrapper.
ECHO [CI stub] For a real build: cd mobile_frontend && npx expo prebuild --platform android && cd android && gradlew assembleDebug
EXIT /B 0

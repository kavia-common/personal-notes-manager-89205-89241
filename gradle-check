#!/usr/bin/env sh
# Root-level CI shim for gradle checks in an Expo-managed repository.
# This script intentionally succeeds and prints guidance.
echo "[CI stub] gradle-check invoked at repo root."
echo "[CI stub] No native Gradle project present (Expo managed workflow)."
echo "[CI stub] For a real Android build:"
echo "  1) cd mobile_frontend"
echo "  2) npx expo prebuild --platform android"
echo "  3) cd android && ./gradlew assembleDebug"
exit 0
